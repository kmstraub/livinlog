<!DOCTYPE html>
<html>
<head>
  <title>Livinlog</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
  <%= yield :head %>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
</head>
<body>
<nav class='navbar navbar-default' role="navigation">
  <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    </div>
 <div class="collapse navbar-collapse navbar-ex1-collapse">
    <a class='navbar-brand' href='/'>LivinLog</a>
    <ul class="nav navbar-nav">
      <li>
      </li>
      <ul class="nav navbar-nav navbar-right">
      <% if current_user %>
        <li><%= link_to "Libraries", users_path %></li>
				<li><%= link_to logs_path do %>
            <span class="glyphicon glyphicon-th"></span> Logs <% end %>
        </li>
        <li class="dropdown"> 
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              <span class="glyphicon glyphicon-plus"></span> New </a>
              <ul class='dropdown-menu'>
                <li><%= link_to "New Log", new_log_path %>
                </li>
              </ul>
            </li>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              <span><%=current_user.name %></span></a>
              <ul class="dropdown-menu">
                <li><%= link_to "My Profile", current_user %></li>
                <li><%= link_to "Log Out", sessions_destroy_path %></li>
              </ul>
  
      <% else %>
      <li class="form-group"><%= form_tag sessions_create_path do %>
        <span><%= text_field_tag :username, nil, placeholder: "username", class: "form-nav" %></span>
        <span><%= password_field_tag :password, nil, placeholder: "password", class: 'form-nav' %><span>
        <span><%= submit_tag "Log In", class: "btn btn-default" %></span></li>
        <% end %>
      <% end %>
    </ul>
  </div>
</div>
  </nav>
  <div class='container'>
    <div class="row">
        <% flash.each do |name, msg| %>
            <%= content_tag :div, msg, class: "flash #{name}" %>
        <% end %>
        <%= yield %>
    </div>
  </div>
  <footer>
    
  </footer>
</body>
</html>

